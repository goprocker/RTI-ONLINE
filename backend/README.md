# RTI Seva backend

This backend is intentionally separate from the frontend and will be implemented later. The current frontend uses only local UI states and does not send or store citizen data.

Planned APIs: authentication and OTP verification, requests and appeals, public authorities, uploads, payments, status timelines, notifications, and audit logs.
